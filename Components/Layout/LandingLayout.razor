@inherits LayoutComponentBase

@* Public marketing page, no sidebar shell (MainLayout) and no auth-card shell (EmptyLayout) - the
   landing page owns its own full-bleed structure end to end. *@
<link rel="stylesheet" href="landing.css" />

@Body

<script src="js/landing.js"></script>
