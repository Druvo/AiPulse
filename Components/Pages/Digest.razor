@page "/digest"
@attribute [Authorize]
@inject NavigationManager Nav

@* Weekly Digest was folded into the Dashboard's Today/Week/Month range switcher - redirect old links/bookmarks there. *@

@code {
    protected override void OnInitialized() => Nav.NavigateTo("/dashboard?range=week", replace: true);
}
