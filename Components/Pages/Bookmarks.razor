@page "/bookmarks"
@attribute [Authorize]
@inject NavigationManager Nav

@* The Reading List was folded into Reading Stats (saved articles + read history in one place) - redirect old links/bookmarks there. *@

@code {
    protected override void OnInitialized() => Nav.NavigateTo("/reading-stats", replace: true);
}
