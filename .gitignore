# Build output
bin/
obj/
publish/
out/

# User data (bookmarks, watchlist, last-visit) — personal, not for source control
App_Data/

# Local/secret config (put your password hash & Obsidian path here)
appsettings.Development.json
appsettings.Local.json
appsettings.Production.json

# Publish/deploy output (binaries + copied App_Data) - not source, may contain real secrets/user data
DeployAiPulse/

# Local deploy automation - reveals internal homelab hostname/IP, not for the public repo
deploy.sh
deploy.bat
aipulse-deploy.tar.gz

# Logs
*.log

# graphify codebase-analysis tool output - local dev aid, not part of the app
graphify-out/

# IDE / OS
.vs/
.vscode/
.idea/
.claude/
*.user
Thumbs.db
.DS_Store
